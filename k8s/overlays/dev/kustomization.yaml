apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

images:
  - name: hello-api
    newName: hello-api
    newTag: latest

replicas:
  - name: go-app
    count: 1

patches:
  - path: serviceaccount-patch.yaml
    target:
      kind: ServiceAccount
      name: app-serviceaccount
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: go-app
      spec:
        template:
          spec:
            containers:
            - name: go-app
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
