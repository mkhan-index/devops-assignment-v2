apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-serviceaccount
  annotations:
    # Update this with the IAM role ARN from Terraform output
    # Run: cd terraform/environments/dev && terraform output iam_role_arn
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/go-app-dev-app-role
